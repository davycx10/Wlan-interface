[project]
name = "wlan-interface"
version = "0.1.0"
description = "Add your description here"
authors = [
    { name = "davycx", email = "davycx.com" }
]
requires-python = ">=3.14"
dependencies = [
    "psutil>=7.2.2",
]

[project.scripts]
wlan-interface = "wlan_interface.main:main"

[build-system]
requires = ["uv_build>=0.12.7,<0.13.0"]
build-backend = "uv_build"
